# Changelog

## 2026-06-25

- Corrected the Vitest config exclude path so `node_modules` is ignored during tests.
- Updated the release workflow to use `npm stage publish` and npm 11.15.0.
- Reordered `shallowDiffers` checks so values are compared before missing keys.
- Disabled package manager caching in the release workflow setup step.
- Added SentDM, Songsterr, and Deno sponsor logos and refreshed sponsor links.